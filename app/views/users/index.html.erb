<div class="leaderboard-container">
  <h1>Leaderboard</h1>
  <ul class="leaderboard">
    <% @users.each do |user| %>
      <li>
        <%= "#{user.username} - #{user.points}" %>
      </li>
    <% end %>
  </ul>
</div>

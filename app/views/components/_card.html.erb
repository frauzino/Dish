<div class="card">
  <% if local_assigns[:header] %>
    <h3 class="card-header"><%= header %></h3>
  <% end %>
  <%= image_tag image[:src], alt: image[:alt], class: "card-image" %>
  <div class="card-content">
    <%= content %>
  </div>
</div>

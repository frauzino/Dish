<!DOCTYPE html>
<html>
  <head>
    <title>Dish. | Make Online Dating Better</title>
    <%= javascript_importmap_tags %>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="Dish is committed to improving the online dating culture through collecting and providing anonymous reports on your dates.">
    <%# fonts %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Prata&family=Yeseva+One&display=swap" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter+Tight:wght@300&family=Prata&family=Yeseva+One&display=swap" rel="stylesheet">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= favicon_link_tag asset_path('dish_favicon.svg') %>
  </head>

  <body data-controller="navbar">
    <div class="desktop-navbar">
      <%= render "shared/navbar" %>
    </div>
    <div class="mobile-navbar">
      <%= render "shared/mobile_navbar" %>
    </div>
    <%= render "shared/flashes" %>
    <%# <div class="master-container"> %>
      <%= yield %>
    <%# </div> %>
  </body>
</html>
